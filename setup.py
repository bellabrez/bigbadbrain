from setuptools import setup, find_packages

setup(
    name="BigBadBrain",
    version="0.0.1",
    long_description=__doc__,
    packages=['BigBadBrain'],
    include_package_data=True,
    install_requires=[],
    zip_safe=False
)